eval "$(rbenv init -)"
eval "$(/Users/andy/hashrocket/hr/bin/hr init -)"

export NVM_DIR="/Users/andy/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"  # This loads nvm

alias cdc="cd ~/code"
alias gld="git log --oneline --decorate --no-merges"
alias cheddar="git commit --amend -CHEAD"
alias qunit="open 'http://localhost:7357'"
